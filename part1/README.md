1. Create the project

```bash
npm create vite@latest unicafe -- --template react
```

2. Install dependencies & start the dev server

```bash
cd unicafe
npm install
npm run dev
```

3. Clean up the boilerplate (Recommended)

```bash
rm -f src/App.css src/index.css src/assets/*
```
